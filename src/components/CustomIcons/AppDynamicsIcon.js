import React from "react";

const AppDynamicsIcon = ({ width = 40, height = 40 }) => {
  return (
    <img
      src="https://www.pikpng.com/pngl/m/561-5611569_appdynamics-logo-png-appdynamics-logo-clipart.png"
      alt="AppDynamics"
      width={width}
      height={height}
      style={{ display: "inline-block", verticalAlign: "middle" }}
    />
  );
};

export default AppDynamicsIcon;